class OptionPanel {
    constructor (homePath) {
        this.homePath = homePath;
    }

    setOptionPanel() {
        document.write('<div id="optionPanel">');
        document.write('</div>');
    }
}